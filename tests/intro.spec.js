// Jest recognizes test files by: files with extension .test.js or .spec.js and all files inside of a __tests__ directory

describe('Sample test', () => {
    it('should test that true === true', () => {
        expect(true).toBe(true);
    });
});
